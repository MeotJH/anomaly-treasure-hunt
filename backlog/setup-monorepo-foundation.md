# setup-monorepo-foundation

## Goal
- Next.js FE와 Nest.js BE를 TypeScript 워크스페이스로 정리한다.
- 계층 구조와 역할별 AGENTS/skills 기준점이 유지되도록 기본 폴더를 고정한다.

## Done Criteria
- 루트 워크스페이스가 존재한다.
- `apps/web`, `apps/api`가 실행 가능한 형태로 구성된다.
- FE/BE 모두 application/domain/infrastructure/presentation 구분이 드러난다.

